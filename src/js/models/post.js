import Backbone from "backbone";

export default Backbone.Model.extend({
	initialize() {
		// what the fuck am i doing
		// window.postroll = window.postroll || {};
		// if (this.get("id")) {
		// 	window.postroll[this.get("id")] = {};
		// 	window.postroll[this.get("id")].caption = this.get("caption");
		// 	window.postroll[this.get("id")].photos = this.get("photos");
		// 	window.postroll[this.get("id")].post_url = this.get("post_url");
		// }
	}
});